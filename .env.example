# Porta do dev server
PORT=3000

# Base da API (vazio = usa /api relativo, servido pelo mock MSW)
NUXT_PUBLIC_API_BASE=

# Adapter de dados: "tanstack" (default) ou "native" (useAsyncData)
NUXT_PUBLIC_DATA_ADAPTER=tanstack
